# Example Environment Configuration for cost-recommendation
# Copy this file to .env.dev, .env.uat, or .env.prd and update with your values

# GCP Project
GCP_PROJECT_ID=your-project-id

# Scope Configuration
# SCOPE_TYPE: 'project', 'folder', or 'organization'
SCOPE_TYPE=project
# SCOPE_ID: Project ID, Folder ID (e.g., '123456789' or 'folders/123456789'), or Org ID (e.g., '987654321' or 'organizations/987654321')
SCOPE_ID=your-project-id

# Firestore Configuration
FIRESTORE_DATABASE=(default)
FIRESTORE_COLLECTION=cost_recommendations

# Recommender Configuration
# Comma-separated list of recommender types to fetch
# Leave empty to fetch ALL available recommender types (recommended)
# Or specify specific types: google.compute.instance.MachineTypeRecommender,google.cloudsql.instance.IdleRecommender
RECOMMENDER_TYPES=

# Filter by recommendation state (ACTIVE, CLAIMED, SUCCEEDED, FAILED, DISMISSED)
RECOMMENDATION_STATE_FILTER=ACTIVE

# Logging
LOG_LEVEL=INFO
