# Production Environment Configuration for cost-recommendation

# GCP Project
GCP_PROJECT_ID=your-prd-project-id

# Scope Configuration
# SCOPE_TYPE: 'project', 'folder', or 'organization'
SCOPE_TYPE=project
# SCOPE_ID: For project mode, use project ID. For folder/org, use folder/org ID
SCOPE_ID=your-prd-project-id

# Firestore Configuration
FIRESTORE_DATABASE=(default)
FIRESTORE_COLLECTION=cost_recommendations

# Recommender Configuration
# Leave empty to fetch ALL available recommender types
RECOMMENDER_TYPES=

# Filter by recommendation state
RECOMMENDATION_STATE_FILTER=ACTIVE

# Logging
LOG_LEVEL=INFO
